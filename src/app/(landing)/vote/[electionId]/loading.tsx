import LoadingSkeleton from '@/components/shared/loading-skeleton';

const Loading = () => {
  return <LoadingSkeleton className="container items-center justify-center" />;
};

export default Loading;
